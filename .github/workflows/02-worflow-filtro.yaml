name: 02 - Worflow Filtro

on: 
  push:
    branches: 'feature/**'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Exibir o evento
        run: echo "Worflow disparado por ${{github.event_name}}"